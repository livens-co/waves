/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'media-public.canva.com',
          'media-private.canva.com'
        ],
      },
}

module.exports = nextConfig
