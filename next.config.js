/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'media-public.canva.com',
          'media-private.canva.com',
          'media.canva.com'
        ],
      },
}

module.exports = nextConfig
